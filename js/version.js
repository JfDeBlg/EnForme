/* EnForme! — source de vérité unique pour le numéro de version.
   Chargé à la fois par index.html (script classique) et par sw.js (importScripts) :
   à chaque session de dev qui modifie un fichier statique, incrémenter APP_VERSION ici,
   le bandeau et le cache du service worker suivent automatiquement. */
const APP_VERSION = "1.1.0";
